{
    "project_name": null,
    "data_type_enum": null,
    "data_type": "{{ 'ai_catalog' if cookiecutter.data_type_enum == '1' else 'flat_file'}}",
    "frontend_enum": null,
    "frontend": "{{ 'react' if cookiecutter.frontend_enum == '1' else 'streamlit'}}",
    "analytics_trace_id": "recipe-forecastic",
    "repo_name": "{{ cookiecutter.project_name.strip().replace(' ', '-').replace('_', '-').lower() }}",
    "python_package": "{{ cookiecutter.project_name.strip().replace(' ', '_').replace('-', '_').lower() }}",
    "kedro_version": "{{ cookiecutter.kedro_version }}",
    "datarobotx_idp_version": "v0.2.10",
    "tools": "{{ cookiecutter.tools }}",
    "example_pipeline": "{{ cookiecutter.example_pipeline }}",
    "_copy_without_render": [
        "*.jinja"
    ]
}